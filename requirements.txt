# TensorFlow and Deep Learning
tensorflow==2.10.0

# Audio Processing
soundfile>=0.10.3
librosa>=0.9.0
wavinfo>=1.0.0
pydub>=0.25.1

# Model Conversion
tf2onnx>=1.13.0
onnx>=1.12.0

# Utilities
tqdm>=4.62.0
numpy>=1.21.0
scipy>=1.7.0

# Optional: For audio resampling
resampy>=0.3.1

# Note: System dependencies (install via apt-get):
# - ffmpeg (for MP3, AAC, M4A support)
# - libsndfile1 (for FLAC, OGG support)
